{# <form method="post" action="{{ path('loan_delete', {'id': loan.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ loan.id) }}">
    <button class="btn">Delete</button>
</form> #}

<a href="{{ path('loan_delete', {'id': loan.id} ) }}" class="text-danger d-flex justify-content-center">Delete this loan</a>
