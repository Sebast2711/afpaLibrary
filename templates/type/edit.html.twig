{% extends 'base.html.twig' %}

{% block title %}Edit Type{% endblock %}

{% block body %}

    <div class="m-3">
        <a href="{{ path('type_index') }}">back to list</a>
    </div>

    <div class="container d-flex justify-content-around align-items-center mt-5 bg-light m-5 p-5">

        <h1>Edit Type</h1>

    {{ include('type/_form.html.twig', {'button_label': 'Update'}) }}

    </div>
    
     {{ include('type/_delete_form.html.twig') }}
{% endblock %}
